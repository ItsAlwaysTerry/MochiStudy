# 过夜自主执行 · 晨间验收

分支：`overnight/review-batch`（**未 push**，main 一动没动，还在 `a1b0995`）
当前构建：`build-20260715n`

## 一句话
安全的都做了（代码在分支上），需要你拍板/需要你眼睛的都写成了文档没瞎改。早上你：① 刷新验代码 → ② 读 3 份决策文档做 2-3 个决定 → ③ 合并你满意的。

---

## A. 已写代码（都在分支上，逐个 commit，可单独回退）

| commit | 任务 | 改了什么 | 怎么验 |
|---|---|---|---|
| `bd19e5a` | Step1 | 首页折叠留痕+卡内瘦身+修对齐/滚动/档案入口 | 首页第一屏应精简；折叠行图标文字对齐；28天点某天不跳顶；"学习档案"入口进卡片册 |
| `d1dd2a4` | #7 | Gemini 往返压到 2 步 + 清微文案 | 例题区只剩"粘贴/文件/复制测验包"3 个按钮（删了冗余复制图）；测验包提示改成"Ctrl+V 直接贴图"；导入框 placeholder 是人话 |
| `226bba6` | #4 | 本节收尾一键 + 今日总复盘可跳过 | 本节收尾只点一下"能做到哪一步"就变绿（不逼打字）；今日总复盘弹窗有"先跳过"按钮，不写也解锁 |
| `c50195a` | #3 | 修暑假卡 hero 数据栏错位 | hero 那排数据（现在 1-2 个）应填满整行、不再留 1/3 空白错位 |

**验的时候重点看**：
- 刷新确认顶部 `build-20260715n`。
- 首页折叠区四个入口对齐是否正了（你之前说图标偏上文字偏下）。
- 28 天总览点任意一天，进度/滚动不跳顶。
- 暑假任务卡整体观感——**这块我只做了"修错位"这种保守打磨，没敢大改样式**（见下"待你的眼睛"）。

## B. 已写文档（等你决定，没动代码）

都在 `docs/review-batch/`：

1. **`reward-decision.md`（#5 奖励，最需要你）** — 真金+概率+near-miss 的去留，给了 A/B/C 三方案 + 推荐 B（里程碑确定奖励，去赌性）。**你回一个 A/B/C 我就改。**
2. **`multi-subject-design.md`（#9 多科）** — 数/物/化 单卡+科目Tab 切换的完整架构、数据迁移、分期。要你确认形态 + 落地顺序。
3. **`task-model-refactor.md`（#6 重构）** — 双任务模型对齐 + 6 个重叠状态收敛的执行计划。**需你在场浏览器验证时做**，没自动化测试不敢过夜跑。
4. **`season-removal-plan.md`（#8 拆赛季）** — 315 处引用，两步法（先搬数据可视化到独立页、再删赛季壳）。要你确认三个问题。

## C. 待你的眼睛 / 待你拍板（清单）

- [ ] **#3 暑假卡深度美化**：我只修了错位，真正"好不好看"要你截图指哪儿糙，我再针对性改。
- [ ] **#5 奖励方向**：A / B / C？（我推荐 B）
- [ ] **#9 多科**：单卡+Tab 形态认可吗？先 #6 再 #9 的顺序认可吗？
- [ ] **#8 拆赛季**：两步法认可吗？后台赛季管理删还是隐藏？旧数据只停用不删（推荐）确认？
- [ ] **#4** 是我按默认方案做的（本节收尾一键 + 总复盘可跳过）。如果你觉得总复盘应该"每周才弹一次"而不是"每天可跳过"，告诉我，我再调。

## D. 没碰的东西（承诺）
- 没删任何用户数据；没改 `study_log` 字段 / 备份结构；没推翻农场/档案。
- 没 push；main 干净。
- #5/#6/#8 一行代码没写，只有文档。

## E. 合并建议
代码那 4 个 commit 你验着没问题，就 `git checkout main && git merge overnight/review-batch`（或挑你要的 cherry-pick）。有任何一个不满意，单独 `git revert <commit>` 即可，互不影响。
