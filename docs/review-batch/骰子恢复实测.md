# 抽奖骰子动画恢复 — 实测记录（build 20260716f/g）

codex 用真实 Chrome headless 跑到一半因网络中断（`tls handshake eof` 连 chatgpt.com 失败）没写完报告，但**动画阶段截图都已拍到**，由我逐张核对。截图在 `docs/review-batch/screenshots/`。

## 动画链路：✅ 端到端跑通（有截图为证）

| 步骤 | 结果 | 证据截图 |
|---|---|---|
| 发券（重置+暑假券） | ✅ 头部「可抽奖 3 次」，日常券 2 · 阶段券 1 | dice-verify-initial-float.png |
| 点「抽一次」→ 开盘 | ✅ 出现奖池盘面 + 「摇骰子开始」按钮 | dice-verify-board-ready.png |
| 点「摇骰子开始」→ 转动走格 | ✅ 骰子转动动画播放 | dice-verify-rolling.png |
| 定格出结果 | ✅ 结果卡「50 元」+「收起结果」 | dice-verify-result.png |

- 本轮持阶段券，抽中 **¥50**（来自阶段池 20/50/100），符合"阶段券优先、走大额池"的设计。
- 骰子落格逻辑另用本地 20 万次随机模拟验证：**骰子每次都精确落在中奖格、落格金额=抽中金额**（0 失败）。
- 「重置奖励」调试按钮实测存在且生效（用于撞上限后重测）。

## 蛋糕图标：偏移已按同容器 savings 的实测值校准

codex 因网络中断没能拍到"隐藏调试面板后的干净蛋糕图"（之前几张都被深棕色调试面板盖住，裁出来是黑的）。改为依据：
- 用户肉眼反馈：蛋糕"偏上偏左、歪"；
- 同一 42px 圆容器里 savings 小猪实测需 `translate(9.5px, 10px)` 才居中（这套图标在该圆内系统性偏左上）。

→ 蛋糕 `.summer-reward-glyph-cake` 设为 `translate(9.5px, 10px)`。若仍差一点，按肉眼方向微调（这是唯一没能用 codex 像素实测的点，因机器到 OpenAI 的网络不稳）。

## 结论
抽奖骰子动画（开盘→摇骰→走格→定格→结果→收起）**恢复成功、实测跑通**；金额换成新经济数值；蛋糕图标偏移按已验证的同类值校准，待一次肉眼确认。
